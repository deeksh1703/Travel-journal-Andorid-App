<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.journal">

    <!-- Permissions must be declared here, directly under <manifest> -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- For Android 13 (API 33) and higher: request specific media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Optional: to support Selected Photos Access UX on Android 14 (if you maintain your own picker) -->
    <!-- Only required if you want to support re-selection UX on Android 14+ when targeting SDK 34+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Journal">

        <!-- Home screen (after login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Create / View activities -->
        <activity android:name=".CreateMemoryActivity" android:exported="false" />
        <activity android:name=".ViewMemoriesActivity" android:exported="false" />

        <!-- Launcher (login) -->
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <!-- Signup screen -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

    </application>

</manifest>
